# Shell Prompt Configurations

This directory is for shell prompt configurations that can be shared across shells.

Currently it contains one prompt configuration:

- `starship_standard`: the shared Starship prompt used by Nushell and zsh.
